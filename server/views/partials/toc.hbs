{{#each categories}}
<p class="h4 margin1--bottom">{{ title }}</h4>
<ul class="margin3--bottom">
  {{#each sections }}
    <li>
      <a class="nav-link{{#if isActive}} nav-link--active{{/if}}" href="/{{ slug }}">{{ title }}</a>
    </li>
  {{/each}}
</ul>
{{/each}}
